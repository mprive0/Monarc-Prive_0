# ══════════════════════════════════════════════════════════
# MONARC PRIVÉ — COMPLETE ENVIRONMENT VARIABLES
# Copy this to .env and fill in all values
# NEVER commit .env to git — add .env to .gitignore
# ══════════════════════════════════════════════════════════

# ── FRONTEND (prefix VITE_ so Vite exposes to browser) ───
VITE_SUPABASE_URL=https://vgmxzkedexjxdtjtbbok.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZnbXh6a2VkZXhqeGR0anRiYm9rIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzY2MjQ1NjUsImV4cCI6MjA5MjIwMDU2NX0.zbCmIPOvIzFRFYTdVsjp1UTFEDl6x1CwqxfaizmwSkw

VITE_STRIPE_PUBLISHABLE_KEY=pk_live_...
# Stripe Price IDs (create in Stripe Dashboard → Products)
VITE_STRIPE_PRICE_MEMBERSHIP_CURATED=price_...     # $300/yr
VITE_STRIPE_PRICE_MEMBERSHIP_PRIVATE=price_...     # $750/yr  
VITE_STRIPE_PRICE_HOST_LISTING=price_...           # $25/mo recurring
VITE_STRIPE_PRICE_AGENT_LISTING=price_...          # $50/mo recurring
VITE_STRIPE_PRICE_EXPERIENCE_LISTING=price_...     # $100/mo recurring

VITE_API_URL=https://monarc-api.onrender.com       # Your backend URL

# ── BACKEND (server/.env) ─────────────────────────────────
# Anthropic (REQUIRED for AI agents)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Supabase (REQUIRED for database)
SUPABASE_URL=https://vgmxzkedexjxdtjtbbok.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...  # Service role - KEEP SECRET

# Stripe (REQUIRED for payments)
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Email / SMTP (REQUIRED for weekly reports and notifications)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=SG.your_sendgrid_api_key_here

# Owner notification email
OWNER_EMAIL=info@monarcprive.com

# App config
NODE_ENV=production
PORT=3001
APP_URL=https://monarcprive.com
ALLOWED_ORIGINS=https://monarcprive.com,https://www.monarcprive.com

# ── OPTIONAL SERVICES ──────────────────────────────────────
# Twilio (SMS notifications)
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...
TWILIO_PHONE_NUMBER=+1...

# Sentry (error monitoring - highly recommended for production)
SENTRY_DSN=https://...@sentry.io/...

# Cloudinary (advanced image optimization, optional)
# Default: uses Supabase Storage (already included)
CLOUDINARY_CLOUD_NAME=monarc-prive
CLOUDINARY_API_KEY=...
CLOUDINARY_API_SECRET=...

# ══════════════════════════════════════════════════════════
# STRIPE SETUP INSTRUCTIONS
# ══════════════════════════════════════════════════════════
#
# 1. Go to dashboard.stripe.com → Developers → API Keys
#    Copy publishable key → VITE_STRIPE_PUBLISHABLE_KEY
#    Copy secret key → STRIPE_SECRET_KEY
#
# 2. Create Products in Stripe → Products → Add Product:
#    a. "Monarc Privé Curated Membership" → $300 one-time
#    b. "Monarc Privé Private Membership"  → $750 one-time
#    c. "Property Listing"                  → $25/mo recurring
#    d. "Agent Advertisement"               → $50/mo recurring  
#    e. "Experience Listing"                → $100/mo recurring
#    Copy each Price ID → paste above
#
# 3. Create Webhook in Stripe → Developers → Webhooks:
#    Endpoint URL: https://your-backend.onrender.com/api/webhooks/stripe
#    Events to listen:
#      - payment_intent.succeeded
#      - payment_intent.payment_failed
#      - customer.subscription.created
#      - customer.subscription.deleted
#      - invoice.payment_failed
#      - charge.dispute.created
#    Copy Signing Secret → STRIPE_WEBHOOK_SECRET
#
# ══════════════════════════════════════════════════════════
# SENDGRID EMAIL SETUP
# ══════════════════════════════════════════════════════════
#
# 1. app.sendgrid.com → Create account
# 2. Settings → API Keys → Create key (Full Access)
# 3. Authenticate your domain (monarcprive.com)
#    Settings → Sender Authentication → Domain Authentication
# 4. Copy API key → SMTP_PASS (SMTP_USER stays as "apikey")
#
# ══════════════════════════════════════════════════════════
